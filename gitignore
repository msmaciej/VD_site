# Dependencies
node_modules/

# Build output — regenerated by `npm run build` / `npm run deploy`,
# published to Hostpoint, and NOT versioned.
dist/
dist-OLD/
deploy/
.build-public/
.build-live/

# Secrets — SERVER ONLY. These must never reach the repo.
# (preview-secret.php, fitcheck-secret.php, …)
*-secret.php

# Local environment & OS cruft
.env
.env.*
.DS_Store
